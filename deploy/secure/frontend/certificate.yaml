apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: chaos-arcade-frontend
  namespace: secure
spec:
  dnsNames:
    - frontend
    - frontend.secure
    - frontend.secure.cluster.local
    - localhost
  secretName: chaos-arcade-frontend-tls
  issuerRef:
    name: self-signed
    kind: ClusterIssuer
